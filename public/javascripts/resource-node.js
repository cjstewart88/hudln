var ResourceNode = function () {
  
  this.properties = {
    x:      Math.floor(Math.random() * 190) * 10,
    y:      Math.floor(Math.random() * 190) * 10,
    value:  Math.ceil(Math.random() * (5 - 1) + 1)
  };

};
